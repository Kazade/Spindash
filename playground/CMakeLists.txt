
FILE(GLOB_RECURSE PLAYGROUND_FILES *.cpp)

PKG_CHECK_MODULES(SDL REQUIRED sdl) 
PKG_CHECK_MODULES(GLU REQUIRED glu) 

INCLUDE_DIRECTORIES(
    ${SDL_INCLUDE_DIRS}
    ${GLU_INCLUDE_DIRS}
)

LINK_LIBRARIES(
    ${SDL_LIBRARIES}
    ${GLU_LIBRARIES}    
)

ADD_EXECUTABLE(spindash_playground ${PLAYGROUND_FILES})

