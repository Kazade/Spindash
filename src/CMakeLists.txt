
PKG_CHECK_MODULES(GL REQUIRED gl)

FILE(GLOB_RECURSE SPINDASH_FILES *.cpp)
FILE(GLOB_RECURSE KAZMATH_FILES ../kazmath/kazmath/*.c)

INCLUDE_DIRECTORIES(
    ${GL_INCLUDE_DIRS}
)

LINK_LIBRARIES(
    ${GL_LIBRARY_DIRS}
)

ADD_LIBRARY(spindash STATIC ${SPINDASH_FILES} ${KAZMATH_FILES}) 
